# FPGABuilder 核心运行时依赖
# 注意：开发依赖在setup.py的extras_require中定义

# 命令行和配置
click>=8.0.0
pyyaml>=6.0
colorama>=0.4.6
toml>=0.10.0
jsonschema>=4.0.0
pydantic>=2.0.0

# 进度显示和用户体验
tqdm>=4.65.0
rich>=13.0.0
prompt-toolkit>=3.0.0
questionary>=2.0.0

# 文件系统和环境
python-dotenv>=1.0.0
watchdog>=3.0.0
pathlib2>=2.3.0; python_version < "3.4"

# Windows特定支持
pywin32>=305; sys_platform == "win32"

# 版本控制和网络（可选功能）
gitpython>=3.1.0
requests>=2.31.0
aiohttp>=3.8.0

# 注意：以下依赖已移动到setup.py的extras_require中：
# - 文档生成 (docs): mkdocs, mkdocs-material, mkdocstrings
# - 测试开发 (dev): pytest, pytest-cov, black, flake8, mypy
# - 打包工具 (packaging): build, pyinstaller, setuptools, wheel
# - GUI支持 (gui): windows-curses (Windows)
# - FPGA工具链 (vivado/quartus): 工具特定依赖

# 构建依赖（用于python scripts/package.py --all）：
# build>=1.0.0
# pyinstaller>=5.0.0
# setuptools>=65.0.0
# wheel>=0.40.0