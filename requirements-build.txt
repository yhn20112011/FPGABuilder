# 构建依赖（用于 python scripts/package.py --all）
# 安装命令: pip install -r requirements-build.txt

# 打包工具
build>=1.0.0
pyinstaller>=5.0.0
setuptools>=65.0.0
wheel>=0.40.0

# 开发工具（可选）
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
mkdocs>=1.5.0
mkdocs-material>=9.0.0
mkdocstrings>=0.23.0

# Windows GUI支持（可选）
windows-curses>=2.3.0; sys_platform == "win32"

# 注意：Inno Setup 是外部工具，需要单独安装
# 下载地址：https://jrsoftware.org/isdl.php